version: '3.8'
services:
  db:
    image: postgres:15
    environment:
      POSTGRES_USER: cafe
      POSTGRES_PASSWORD: cafe_password
      POSTGRES_DB: cafe_erp
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
